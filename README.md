# Gita

A calm, minimal Bhagavad Gita mobile app built with React Native and Expo.

## Run

```bash
npm install
npx expo start
```

Copy `.env.example` to `.env` and set `GEMINI_API_KEY` before using Krishna chat.
